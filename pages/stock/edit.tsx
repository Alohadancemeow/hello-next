import React from 'react'
import Layout from '../../componrnts/layout'

interface Props {
    
}

const StockEdit = (props: Props) => {
    return (
        <Layout>
            <h1>Edit</h1>
        </Layout>
    )
}

export default StockEdit
